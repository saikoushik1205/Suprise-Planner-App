MONGODB_URI=
JWT_SECRET=
PORT=5000
# Exact browser origin allowed to call this API. Production rejects every other origin.
# After the web app is deployed, set this to that site's origin (no trailing slash).
CLIENT_URL=http://localhost:8081
